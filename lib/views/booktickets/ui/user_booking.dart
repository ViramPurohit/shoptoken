import 'package:flutter/material.dart';

class UserBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      // drawer: HomeScreen(),
      appBar: AppBar(
        title: Text('UserBookings'),
      ),
    );
  }
}
